#include <bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int t;
    cin>>t;
    int i;
    for(i=0;i<t;i++)
    {
        string s;
        cin>>s;
 
        sort(s.begin(),s.end());
        cout<<s<<endl;
 
 
    }
 
}